function set_Bpod_baudrate(port, baudrate)
    % Check that the port is not currently being used
    % Check that the baudrate is within limits for RS232
    % Set the baudrate, then double check that the setting was set
end